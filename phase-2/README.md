## Phase 2 – Model Training

In Phase 2, the preprocessed review data was converted into numerical form using TF-IDF vectorization and used to train different Machine Learning models such as Logistic Regression, KNN, and SVM. The dataset was divided into training and testing sets, and the models were evaluated using accuracy, precision, recall, and F1-score to check their sentiment prediction performance.
